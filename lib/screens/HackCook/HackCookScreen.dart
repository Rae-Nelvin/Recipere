import 'package:flutter/material.dart';

class HackCookScreen extends StatelessWidget {
  const HackCookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("This is Hack Cook Screen");
  }
}
